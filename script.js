const Gameboard = () => {
    const 
}

const CreatePlayer = (marker) => {
    const marker = marker;
    const move = (selection) => {
        // update dom element and update gameboard
    }
    return { marker }
}